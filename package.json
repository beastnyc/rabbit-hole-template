{
  "name": "fieldnotes-template",
  "version": "1.0.0",
  "private": false,
  "description": "A static field notes site with contextual note panes, Obsidian-style links, and public/private publishing guardrails.",
  "scripts": {
    "dev": "vite",
    "build": "node _site/build.js && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "front-matter": "^4.0.2",
    "marked": "^12.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
